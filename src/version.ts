/**
 * Single source of truth for the AutoLayout PRO version this docs site targets.
 *
 * Bump this string at the same time as the package's `package.json` version.
 * Used in the footer (visible to readers so they know which API the docs cover)
 * and in the changelog page.
 */
export const PACKAGE_VERSION = '1.0.0';

/** Pre-release / beta status — shown as a badge alongside the version when truthy. */
export const PACKAGE_STATUS = '';
